<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/lux/bootstrap.min.css" rel="stylesheet">
    <title>{% block title %}Admin - SymHome{% endblock %}</title>
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark shadow-sm" style="background-color: #5D4037;">
    <div class="container">
        <a class="navbar-brand fw-bold" href="#">Admin Panel</a>
        <div class="d-flex gap-2">
            <a href="{{ path('admin_meuble_list') }}" class="btn btn-outline-light btn-sm">Meubles</a>
            <a href="{{ path('admin_categorie_list') }}" class="btn btn-outline-light btn-sm">Catégories</a>
        </div>
    </div>
</nav>

<div class="container py-5">
    {% block body %}{% endblock %}
</div>

</body>
</html>